version: '3.8'

services:
  transparencia-ws:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: transparencia-caninde-ws
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      PORT: 8080
      # WS_AUTH_TOKEN: "defina-um-token-secreto"  # opcional: exige ?token= no WebSocket
      RATE_LIMIT_MAX: 120
      RATE_LIMIT_WINDOW_MS: 60000
    restart: unless-stopped
    networks:
      - transparencia-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    volumes:
      - ./server/logs:/app/logs

networks:
  transparencia-net:
    driver: bridge

volumes:
  logs:
